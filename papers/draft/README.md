No manuscript yet. Do not invent a title page, abstract, or numerical results.

The intended pipeline is:

```text
config → run → results/*.json → figures/tables → draft
```

A possible later direction (only if experiments support it):

> Sequential experiment selection for scientific discovery under limited budgets.

Update `CITATION.cff` when a preprint exists.
