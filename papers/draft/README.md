No manuscript yet. Do not invent a title page, abstract, or numerical results by hand.

The pipeline is:

```text
config → run → results/*.json → figures/tables → docs/reports → draft
```

Experiment reports in `docs/reports/` are filled from `summary.json`. A possible later direction (only if a coherent story survives those reports):

> Sequential experiment selection for scientific discovery under limited budgets.

Update `CITATION.cff` when a preprint exists.
