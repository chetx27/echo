# Causal comparison

**Status:** to be filled from `results/experiment_causal/summary.json` after

`python -m echo compare --config configs/experiment_causal.yaml`

If this file still says "not yet run", do not invent numbers.

## Question

Under the hidden SCM \(A\to C\to D\), \(B\to C\) and candidates \(\mathrm{do}(A=a,B=b)\), which sequential policy recovers the graph (structural Hamming distance of an evaluator-only BIC parent search) more efficiently than random or uncertainty sampling?

This is not a claim that ECHO is a causal-discovery method.
